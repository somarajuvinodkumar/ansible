---
- name: python install and run
  hosts: local
  become: yes
  tasks:
    - name: install python server
      ansible.builtin.package:
        name: python
        state: present
    - name: start python
      ansible.builtin.service:
        name: python
        state: started
        enabled: yes
