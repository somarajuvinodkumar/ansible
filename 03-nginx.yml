---
- name: nginx install and run
  hosts: local
  become: yes
  tasks:
    - name: install python server
      ansible.builtin.package:
        name: python
        state: present
    - name: start nginx
      ansible.builtin.service:
        name: nginx
        state: started
        enabled: yes
